# Python HW2

## Потрібні знання для рішення задач
- [ ] [https://www.w3schools.com/python/python_conditions.asp]
- [ ] [https://www.w3schools.com/python/python_while_loops.asp]
- [ ] [https://www.w3schools.com/python/python_for_loops.asp]

# 1. Як вхідні дані запитайте ціле число. Якщо воно ділиться на 3, виведіть "foo"; якщо воно ділиться на 5, виведіть "bar"; якщо воно ділиться на обидва, виведіть "ham" (а не "foo" або "bar").

# 2. Як вхідні дані запитайте два числа та виведіть яке з них менше і яке більше

# 3. Як вхідні дані запитайте три числа і виведіть найменше, середнє та найбільше. Припустимо, всі вони різні

# 4. Зіграйте у гру Fizz-Buzz: виведіть усі числа від 1 до 100; якщо число ділиться на 3, замість числа виведіть "fizz". Якщо воно ділиться на 5, замість числа виведіть "Buzz". Якщо воно ділиться на обидва, виведіть "fizz buzz" замість числа.

# 5. Зіграйте у гру 7-boom: виведіть усі числа від 1 до 100; якщо число ділиться на 7 або містить цифру 7, виведіть "BOOM" замість числа.


# Рекомендації до виконання:
## 1 завдання
У цьому коді відбувається наступне:
1)   Користувач вводить значення з клавіатури за допомогою функції input. Використовуючи функцію int, програма конвертує отримане значення у ціле число. Наприклад: 
a = int(input('Input integer: '))
2)   Перевіряється, чи введене число ділиться на 3 та 5 без залишку. Якщо так, виводиться слово "ham".
3)   Якщо перше умова не виконалась, перевіряється, чи введене число ділиться на 3 без залишку. Якщо так, виводиться слово "foo".
4)   Якщо і друга умова не виконалась, перевіряється, чи введене число ділиться на 5 без залишку. Якщо так, виводиться слово "bar".
5)   Якщо жодна з перевірок не виконалась, виводиться повідомлення "не ділиться ні на 3, ні на 5".
Рішення 1 завдання можна дещо ускладнити додаванням перевірки на те, чи введене значення є числом. У такому випадку матимемо:
1)   Виконується безкінечний цикл while, який продовжується до тих пір, поки користувач не введе правильне ціле число.
2)   Користувачу виводиться запит на введення значення за допомогою функції input. Використовуючи функцію int, програма конвертує отримане значення у ціле число.
3)   Перевіряється, чи введене значення є цілим числом за допомогою методу isdigit(). Якщо так, воно конвертується у ціле число за допомогою функції int, і цикл завершується за допомогою break.
4)   Дивися кроки 2-5 вище.

## 2 завдання
У цьому коді відбувається наступне:
1)   Користувач вводить два значення за допомогою функції input. Використовуючи функцію int, програма конвертує отримані значення у цілі числа.
2)   Програма перевіряє, чи перше введене число більше за друге. Якщо так, виводиться повідомлення, що перше число більше за друге.
Якщо перше число менше за друге, виводиться повідомлення, що перше число менше за друге. Якщо обидва числа рівні, виводиться повідомлення, що вони рівні.
Рішення 2 завдання можна дещо ускладнити додаванням перевірки на те, чи введені значення є числами. У такому випадку матимемо:
1)   Виконується безкінечний цикл while, який продовжується до тих пір, поки користувач не введе два цілих числа.
2)   Користувачу виводяться повідомлення про введення першого та другого числа за допомогою функції input.
3)   Перевіряється, чи введені значення є цілими числами за допомогою методу isdigit(). Якщо так, вони конвертуються у цілі числа за допомогою функції int, і цикл завершується за допомогою break.
4)   Дивися крок 2 вище.
 
У 2 завданні також можна використати рандомні числа:
1)   Генеруються два випадкові числа в діапазоні від 100 до 1000 за допомогою функції random.randint.
2)   Отримані числа зберігаються у списку.
3)   Перевіряється, чи значення першого елементу списку не дорівнює значенню другого елементу. Якщо це так, виводиться повідомлення про те, яке з чисел є більшим.
Якщо значення першого елементу списку рівне значенню другого елементу, виводиться повідомлення про рівність цих чисел.
 
*** На майбутнє: є спосіб як ввести відразу багато чисел у 2 завданні:
a, b = map(int, input("Enter two numbers: ").split(" "))
У такому випадку матимемо:
1)   Користувачеві пропонується ввести два числа за допомогою функції input. Введені дані розділяються за допомогою пробілу та конвертуються у цілі числа за допомогою функції map. Значення a та b присвоюються введеним числам.
2)   Створюється список, який містить ці значення.
3)   Перевіряється, чи значення a не дорівнює значенню b. Якщо це так, виводиться повідомлення про те, яке з чисел є більшим.
print(f'{max(список)} > {min(список)}')
Якщо значення a рівне значенню b, виводиться повідомлення про рівність цих чисел.

## 3 завдання
У цьому коді відбувається наступне:
1)   Користувачеві пропонується ввести три значення за допомогою функції input. Використовуючи функцію int, програма конвертує отримані значення у цілі числа.
2)   За допомогою функцій max() та min() знаходимо максимальне та мінімальне значення серед введених чисел.
3)   Медіанне значення (яке залишилося) обчислюється як сума всіх трьох введених чисел, з якої віднімається максимальне та мінімальне значення.
4)   Отримані результати виводяться на екран за допомогою функції print.
Інший спосіб рішення:
1)   Користувачеві пропонується ввести три значення за допомогою функції input. Використовуючи функцію int, програма конвертує отримані значення у цілі числа.
2)   3 числа зберігаються у списку.
3)   Запускається цикл for, який ітерується по кожному елементу списку.
Для кожного елементу перевіряється, чи він менше за найменше число у списку і більше за найбільше число у списку. Якщо це виконується, значення елементу приймається як медіанне значення.
Виводиться повідомлення за допомогою функції print, яке містить мінімальне, медіанне та максимальне значення у списку.
Рішення 3 завдання можна дещо ускладнити додаванням перевірки на те, чи введені значення є числами. У такому випадку матимемо:
1)   Використовується тричі цикл while, щоб запитати користувача ввести три числа. Кожен раз перевіряється, чи введене значення є цілим числом.
2)   Кожне введене значення конвертується в ціле число та присвоюється відповідній змінній.
3)   За допомогою функцій max() та min() знаходяться найбільше і найменше значення серед введених чисел.
4)   Медіанне значення обчислюється як сума усіх трьох введених чисел, з якої віднімаються максимальне та мінімальне значення.
5)   Виводяться знайдені значення максимуму, мінімуму та медіани за допомогою функції print.
 
У 3 завданні також можна використати рандомні числа:
1)   У даній програмі генерується список за допомогою спискового виразу та функції random.randint(), що складається з трьох випадкових цілих чисел в діапазоні від 100 до 1000 включно:
numbers = [random.randint(100, 1000) for _ in range(3)]
2)    Потім цей список сортується за зростанням за допомогою методу sort()
3)   Виводимо на екран найменше, медіанне та найбільше число зі списку.
 
*** На майбутнє: є спосіб як ввести відразу багато чисел у 3 завданні:
numbers = list(map(int, input("Enter three numbers: ").split(" ")))
У такому випадку матимемо:
1)   Користувачеві пропонується ввести три числа за допомогою функції input(). Введені значення розділяються пробілами за допомогою методу split(" "), і результат конвертується в список цілих чисел за допомогою функції map(int, ...).
2)   Знаходиться мінімальне значення у списку за допомогою функції min().
3)   Знаходиться максимальне значення у списку за допомогою функції max().
4)   Медіанне значення обчислюється як сума всіх чисел мінус мінімальне і максимальне значення.
5)   Виводяться мінімальне, середнє і максимальне значення за допомогою функції print().
Інший підхід при вводі
1)   numbers = list(map(int, input("Enter three numbers: ").split(" ")))
2)   Список чисел сортується за зростанням за допомогою методу sort().
3)   Виводяться найменше, медіанне і найбільше число за допомогою функції print().
Ще інший підхід при вводі
1)   numbers = list(map(int, input("Enter three numbers: ").split(" ")))
2)   Зі списку чисел знаходяться мінімальне та максимальне значення за допомогою функцій min() і max() відповідно.
3)   Знаходиться медіана списку чисел за допомогою функції statistics.median(). Для цього треба імпортувати бібліотеку statistics:
import statistics
 
## 4 завдання
Цикл for виконується для кожного числа від 1 до 100.
Для поточного числа перевіряється, чи воно кратне 3, 5 або обом за допомогою оператора %:
1)   Якщо число кратне обом, виводиться 'fizz buzz'.
2)   Якщо кратне лише 3, виводиться 'fizz'.
3)   Якщо кратне лише 5, виводиться 'buzz'.
4)   У всіх інших випадках виводиться саме число.
Відповідно до результату перевірки виводиться відповідне повідомлення або саме число.
5 завдання
Цикл for виконується для кожного числа від 1 до 100.
Для кожного числа перевіряється, чи воно кратне 7 або чи містить цифру '7' у своєму представленні як рядок за допомогою операторів % та in:
if число % 7 == 0 or '7' in str(число)
Якщо умова виконується, виводиться 'BOOM'. У протилежному випадку виводиться саме число.